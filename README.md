# MEG-BDH

A project that reads brain signals (MEG) recorded while someone listens to speech, and tries to recover phonemes, words, or meaning.

## Layout

See the folders under `configs/`, `src/`, `scripts/`, `tests/`, `data/`, `experiments/`, `outputs/`, and `docs/`. Each file starts with a short note about what it is for.
